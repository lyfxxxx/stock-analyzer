# Stock Analyzer Environment Variables

# Live API Integration Tests
# Set to 'true' to enable tests that make real HTTP requests to East Money API
# Default: undefined (tests are skipped to avoid rate limiting)
VITE_LIVE_API_TESTS=false

# Note: Enable this only when you need to verify real API integration.
# Running live API tests frequently may trigger rate limits from East Money.
